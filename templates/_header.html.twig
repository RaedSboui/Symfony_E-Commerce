<div class="wrapper">
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <div id="logo" class="navbar-brand">
        <h1>COSMETIC <sub>SR</sub> SHOP</h1>
      </div>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarColor02">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link active" href="{{ path('home') }}">Home</a>
          </li>
          {% if is_granted('ROLE_ADMIN')  %}
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Product</a>
            <div class="dropdown-menu">
              <a class="dropdown-item" href="{{ path('product_index') }}">List</a>
              <a class="dropdown-item" href="{{ path('product_new') }}">Create new</a>
            </div>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Category</a>
            <div class="dropdown-menu">
              <a class="dropdown-item" href="{{ path('category_index') }}">List</a>
              <a class="dropdown-item" href="{{ path('category_new') }}">Create new</a>
            </div>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Customer</a>
            <div class="dropdown-menu">
              <a class="dropdown-item" href="{{ path('customer_index') }}">List</a>
              <a class="dropdown-item" href="{{ path('customer_new') }}">Create new</a>
            </div>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Order</a>
            <div class="dropdown-menu">
              <a class="dropdown-item" href="{{ path('order_index') }}">List</a>
              <a class="dropdown-item" href="{{ path('order_new') }}">Create new</a>
            </div>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Order Line</a>
            <div class="dropdown-menu">
              <a class="dropdown-item" href="{{ path('order_line_index') }}">List</a>
              <a class="dropdown-item" href="{{ path('order_line_new') }}">Create new</a>
            </div>
          </li>
          {% endif %}
          <li>
            <a class="nav-link" href="{{ path('cart_index') }}"><i class="fas fa-shopping-cart"></i></a>
          </li>
        </ul>
        <div class="d-flex">
          {% if not app.user %}
          <a href="{{ path('security_registration') }}" class="btn btn-primary" style="margin-right: 1rem">Register</a>
          <a href="{{ path('security_login') }}" class="btn btn-success">Login</a>
          {% else %}
          <a href="{{ path('security_logout') }}" class="btn btn-danger">Logout</a>
          {% endif %}
      </div>
      </div>
    </div>
  </nav>
<div>