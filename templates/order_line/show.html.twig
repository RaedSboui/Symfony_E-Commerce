{% extends 'base.html.twig' %}

{% block title %}OrderLine{% endblock %}

{% block body %}
<div style="margin-top:5rem;">
    <h1 class="text-center" style="margin-bottom: 2rem;">
Order Line <span style="color:#e74c3c"> N° {{order_line.id}}</span>
    </h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_line.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ order_line.amount }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ order_line.price }}</td>
            </tr>
        </tbody>
    </table>
    <table class="mx-auto w-auto">
        <tr>
            <td>
                <a href="{{ path('order_line_index') }}" class="btn btn-primary" style="margin-right: 1rem;">
                    <i class="fas fa-arrow-left"></i> Back
                </a>
            </td>
            <td>
                <a href="{{ path('order_line_edit', {'id': order_line.id}) }}" class="btn btn-success" style="margin-right: 1rem;">
                    <i class="fas fa-edit"></i>
                </a>
            </td>
            <td>
                {{ include('order_line/_delete_form.html.twig') }}
            </td>
        </tr>
    </table>
</div>
{% endblock %}
